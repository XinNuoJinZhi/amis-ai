# 1.6 W4 接力文档（2026-05-21 → 2026-06-11）

> **来源**：W3+ 冷启动 fix 完成（最新 push: `2ec867e`），1.6 进入 **数据观察期 + B v2 实施期**
> **日期**：2026-05-21
> **前传**：[2026-05-13-roadmap-1.6.md](2026-05-13-roadmap-1.6.md)（1.6 路线图，scope = A+B+G+C）
> **来由**：会话结束前没显式落「接力文档」到 repo，下次接力的人看 memory 不一定及时；本文档定位「打开 5 秒就能知道现在在哪 + 下一步该干嘛」

---

## 一、1.6 当前状态（截至 2026-05-21）

### dev/1.6.0 commit 速览（最近 5 个）

| commit | 日期 | 内容 |
|---|---|---|
| `2ec867e` | 5-21 | docs(1.6 W3+): 冷启动 fix 收尾文档 + B v2 召回敏感评测草案 |
| `54cb06b` | 5-21 | feat(1.6 W3): 全栈 health 身份探测 + baseline 2026-05-21 |
| `fa459a4` | 5-21 | fix(1.6 W3): 修分类器冷启动 NULL — health JSON + wait_for_health |
| `f5c030a` | 5-13 | refactor(scaffold/zc-editor): 临时名 → AppRoot.tsx (修大小写冲突 step 2/2) |
| `f9d008c` | 5-13 | refactor(scaffold/zc-editor): App.tsx → 临时名 (修大小写冲突 step 1/2) |

### 1.6 scope 进度

| 轨道 | 状态 | 备注 |
|---|---|---|
| **A** page-bad 自动入库 | ✅ 代码落地（`d717a65`），灰度未开 | 总闸 `rag.judge.auto_page_negative=false`，等业务跑稳 |
| **B** 召回敏感评测集 | 🟡 v1 实证 **B-A=+4pp**，v2 草案已写 | v2 需 1-2 天扩 5-8 prompts，目标 +10pp |
| **G** smoke + regression | ✅ smoke §11（3 assert）+ regression §10（5 项）已加 | 用户 5-13 跑过 smoke PASS=77 |
| **C** 数据观察 | 🟡 baseline 1 已记（5-21），后续 5-28 / 6-04 / 6-11 三次 | calibration_factor 已 = 140 |
| **W3 收尾** | ✅ 冷启动 fix（`fa459a4` + `54cb06b`）— 多年技术债 | 修了"重启日 NULL 大量产生"真因 |

### 关键数据指标（30 天）

```
total tasks:      256
succeeded:        218 (85.2%)
__null__:         122 (47.6%)  ← 全是重启日产生的，wait_for_health fix 后预期 < 10%
oa_form ratio:    0.93 ✅
named ratio 范围:  [0.54, 0.93]（系数 140 略保守，6-04 再评估）
```

---

## 二、下一步 action items（按日期排）

### 📅 2026-05-28（一周后）— baseline 2 验证 + A 灰度时机

```bash
# 1. 跑 baseline SQL，记 docs/upgrades/baseline-2026-05-28.md（模板套 baseline-2026-05-21.md）
PGPASSWORD=amis_ai_dev psql -h localhost -U amis_ai -d amis_ai -f docs/upgrades/2026-05-13-1.6-c-data-baseline.md 内嵌 5 段 SQL

# 2. 重点观察
#   - __null__ 占比是否从 47.6% 降到 < 10%（验证 wait_for_health 真的解决了冷启动）
#   - ratio 是否仍在 [0.54, 0.93]（系数 140 还稳？）
#   - 是否出现新 category 上量（business 数据流量分布）
```

**决策点**：
- ✅ NULL 占比 < 10% + ratio 稳 → 准备开 A 灰度：admin PUT
  ```
  rag.judge.page_mode             = auto_on_complete
  rag.judge.auto_page_negative    = true
  rag.judge.auto_page_min_confidence = 0.85   # 保守阈值
  rag.judge.auto_page_kind        = structural
  ```
  开启后观察 `code_samples WHERE source_page_id IS NOT NULL AND status='pending_review'` 的增长
- ❌ NULL 仍偏高 → 调查冷启动以外的 NULL 来源（grep agent.log 找 classify 失败 case）

### 📅 2026-06-04（两周后）— B v2 实施 + W3 A/B 启动

#### B v2 实施（草案已落 [eval/1.6-recall-sensitive-v2/README.md](../../eval/1.6-recall-sensitive-v2/README.md)）

```bash
# 1. 按 README 第二节"v2 升级路线"扩展 prompts.json（rsv2_2 到 rsv2_8）
vim eval/1.6-recall-sensitive-v2/prompts.json

# 2. 跑 A/B 对照
./shared/scripts/start-services.sh start   # 自带 wait_for_health 不会再踩冷启动
TOKEN=$(curl ... 拿 token)
# 关 B.1
curl -X PUT .../rag.dual_route.enabled -d '{"value":"false"}'
TEST_ADMIN_JWT=$TOKEN python3 eval/multipage-1.2/runner.py \
  --prompts-file eval/1.6-recall-sensitive-v2/prompts.json --strategies r4_baseline ...
# 开 B.1
curl -X PUT .../rag.dual_route.enabled -d '{"value":"true"}'
... 同上

# 3. diff 两份 CSV，统计 page_status PASS 数差
#    目标：B-A ≥ +10pp（24 pages × 10% = 至少多 3 个 PASS）
```

#### W3 A/B 框架启动（仅在 baseline 2 NULL 占比 OK 时）

```bash
# admin 配 B 组覆盖表（B 组用更激进的 frontier）
curl -X PUT .../llm.routing.category_tier_overrides_json_b -d '{
  "value": "{\"static_page\":\"fast\",\"oa_form\":\"frontier\",\"zc_business\":\"frontier\"}"
}'
# 开总闸（建议先 1%/99% 灰度，需要改 compute_ab_variant 临时阈值；或直接 50/50）
curl -X PUT .../llm.routing.ab_test_enabled -d '{"value":"true"}'

# 跑两周 → 6-18 看 /api/admin/ab-compare 数据
```

### 📅 2026-06-11（四周后）— 1.6 总收尾

1. 跑 baseline 4 → 写 `docs/upgrades/2026-06-11-1.6-final.md`：
   - 4 次 baseline 趋势对比图
   - A 灰度产出（page-bad 入库样本数）
   - B v2 实测（B-A 差最终值）
   - W3 A/B 优胜组数据
   - calibration_factor 最终值（仍 140？调到 180？）
2. PR `dev/1.6.0 → main` + tag `v1.6.0`
3. 起草 1.7 路线图（E admin UI / D Tauri / F Marketplace 三选）

---

## 三、留给 1.6 W4 接力者的 cheat sheet

### 当前已就位

- 4 服务都返回 `{"service":"<name>","status":"ok"}` JSON health
- `start-services.sh` 自带 `wait_for_health × 4`（agent 真冷启动窗口 3s 已兜住）
- `start-services.sh status` 全栈身份探测（worksy 误占端口立即报警）
- DB 已加列：`ab_variant`, `accumulated_cost_tokens`, `source_page_id`
- Settings 已加：`rag.dual_route.enabled` / `llm.routing.ab_test_enabled` / `llm.routing.category_tier_overrides_json_b` / `llm.quota.estimate_calibration_factor=140` / `rag.judge.auto_page_negative=false` 等

### 跑评测姿势（参考今天实证 fa459a4）

```bash
./shared/scripts/start-services.sh start  # 等 4 个 warmed 标志
TOKEN=$(curl -s http://localhost:8080/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"admin123"}' \
  | python3 -c 'import sys,json; print(json.load(sys.stdin)["token"])')
echo "$TOKEN" > /tmp/admin_token.txt
```

### Memory 速查（下次 session 重要）

- [[agent_cold_start_warmup]] — 冷启动 NULL 真因 + fix 设计
- [[roadmap_1_x]] — 1.x 全版本进度（含 1.6 W3+ 状态）
- [[b1_keyword_design]] — B.1 双路召回历史 + 1.5 W4 多页 fix
- [[feedback_autonomous_push_restart]] — 1.6 起 push/restart/admin setting 自主处理

---

## 四、留 1.7 的事

- **E** Admin UI 数据可视化（依赖 1.6 C 数据积累，是消费层）
- **D** 桌面端 Tauri 模板（3-4 周大工程）
- **F** Skills Marketplace MVP（4 周独立项目）

## 五、不做的事

1. 不擅自开 A 灰度（确保业务跑稳一周）
2. 不擅自启 A/B 实验（确保 baseline 2 NULL 已降）
3. 不动 1.5 W2 校准系数（140 已稳，6-11 再评估是否调 180）

---

## 六、关联文档

- [1.6 路线图初稿](2026-05-13-roadmap-1.6.md)
- [1.6 W2-W3 总结](../upgrades/2026-05-13-1.6-w2-w3-summary.md)
- [1.6 W3+ 冷启动 fix](../upgrades/2026-05-21-1.6-w3-coldstart-fix.md)
- [baseline 2026-05-21](../upgrades/baseline-2026-05-21.md)
- [B v2 草案](../../eval/1.6-recall-sensitive-v2/README.md)
