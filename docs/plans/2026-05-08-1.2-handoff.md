# 1.2.0 接力文档（next session 看这份就够）

> **状态**：1.2 代码框架完成 + 数据流通了 + 4 项缺陷已全修；剩 W7 评测 + 合 main + tag。
> **分支**：`dev/1.2.0`（接力时 23 commits 领先 main，缺陷修复后会再加 1-2 commits）
> **配套**：[2026-05-08-multipage-reverse-flywheel-design.md](2026-05-08-multipage-reverse-flywheel-design.md) / [2026-05-08-multipage-reverse-flywheel-plan.md](2026-05-08-multipage-reverse-flywheel-plan.md)

## 已落地（1.2 代码框架）

| Phase | 内容 | 测试 |
|---|---|---|
| W1（5 task）| DB schema + entity + /route-infer + create_task 多页 + 前端输入页 | 1.1 13 + 1.2 3 |
| W2（5 task）| multipage_scheduler 调度器骨架 + R4 baseline + Semaphore 限流 + events vocab | Semaphore 2 |
| W3（3 task）| global_prompt_builder + R2 prompt 注入 | — |
| W4（5 task）| R1 骨架先行三阶段 + scaffold_skeleton.md prompt | — |
| W5（5 task）| R3 后处理重构 + 统筹模式 + 2 prompt | — |
| W6（4 task）| sandbox reuse_metrics + PagesPanel + multipage_recorder + RAG strategy filter | sandbox 3 + multipage_recorder 3 + RAG filter 3 |
| 现场 fix | TIMESTAMPTZ→TIMESTAMP（schema）+ create_task 多页 dispatch 分发 | — |
| **2026-05-08 缺陷修** | **真完成等待 + 闭环连线 + 时区统一 + e2e jq 守护** | **+ multipage_reuse 4** |

总计 **23 commits + 缺陷修 / 33 测试全绿 / cargo build / tsc 0 error / 5 个 e2e 脚本就位**。

---

## ✅ 缺陷修复完成（2026-05-08 session）

### ✅ 缺陷 1：「session 真完成」等待逻辑已加

**新模块** [`backend/src/services/multipage_session_watcher.rs`](../../backend/src/services/multipage_session_watcher.rs)：
- 订阅 `ws://{claw_agent_url}/tasks/{session_id}/events`
- 把每条事件以原文 JSON 落库到 `project_task_event`（关联 backend 的 multipage task_id），让前端 history REST 可回放每页 session 内细节
- 同时按 tracelog 配置归档（与单页路径一致）
- 看到 `status_change=succeeded/failed/stopped` 才返回；30 分钟超时兜底

**接入点**（5 处）：
- `run_unified` 后等 → 真完成才标 5 页 done
- `run_skeleton_stage` 后等 → 骨架真生成才让阶段 2 各页并发（不再扫到空骨架）
- `run_cleanup_stage` 后等 → cleanup 真完成才记 cleanup_done
- `run_r3_refactor` 后等 → 重构真完成才记 refactor_done
- `run_isolated_pages_with_shared_context` joinset 内每页 spawn 后等 → 不再 64ms 假完成

### ✅ 缺陷 4：闭环连线已加

**新增** `multipage_scheduler::run_epilog`（dispatch 末尾自动调用）：
1. 用 [`backend/src/services/multipage_reuse.rs`](../../backend/src/services/multipage_reuse.rs)（不依赖 regex 的纯 str 扫描，与 sandbox/src/reuse_metrics.rs 行为对齐）算 shared/ 复用率
2. 写一条 `reuse_metric` event（带 import_count / file_count / reuse_rate / strategy 四元组）
3. 成功任务才调 agent `POST /multipage/record` 写 RAG（status=pending 等人审）；失败任务跳过避免脏样本
4. 写 `rag_recorded` 或 `rag_record_failed` event 留痕

**新增** `AppState.agent_url` 字段（env `AGENT_URL`，默认 `http://localhost:8000`）。

**4 个单元测试** 在 `backend/src/services/multipage_reuse.rs` 全绿：zero / shared imports / dir missing / two-layer depth。

### ✅ 缺陷 2：时区已全局统一

**全局**把 `backend/src/` 下的 `chrono::Local::now().naive_local()` 替换为 `chrono::Utc::now().naive_utc()`（30+ 处）。同步修了 [`backend/src/handlers/project_generation.rs`](../../backend/src/handlers/project_generation.rs) `workdir_changed_since_task_start` 里的 `chrono::Local.from_local_datetime` → `task_created_at.and_utc().timestamp()` 直接还原。

**前端展示**自行做时区转换（DB 都是 UTC naive，前端按 UTC 解读 + 转本地展示是统一约定）。1.1 旧 task 行（DB 里是 Local naive）在 dev_status_watcher 防御里会被早 8 小时解读，但旧任务都已结束、不再走该分支，影响仅限于"重启 backend 后还在 running 的旧 task"。

### ✅ 缺陷 3：e2e jq null 守护已加

只有 [`eval/multipage-1.2/test_e2e_r1.sh:52`](../../eval/multipage-1.2/test_e2e_r1.sh) 一行用了 `startswith` —— 已改成 `select((.event_type // "") | startswith(...))`。其他 4 个脚本用的是 `==` 等值比较，与 null 比较返回 false 不报错，无需改。

---

## 待做的（plan W7 + 上线前）

按优先级：

1. **回归冒烟**：起 4 服务，跑 `./shared/scripts/start-services.sh restart` + 5 个 e2e 验证缺陷修复闭环（每页 started_at→finished_at 应该是真耗时，不再 64ms；任务结束后 events 表里要有 `reuse_metric` + `rag_recorded`）
2. **W7.1**：写 `eval/multipage-1.2/runner.py` + `prompts.json`（5 策略对比 runner）
3. **W7.2**：4 服务在线 + admin JWT，跑 5 策略对比，导出复用率 / 通过率 / token 消耗矩阵
4. **W7.3**：PR 合 main + tag v1.2.0

## 启动条件（next session 跑 e2e 时）

```bash
# 1. 4 服务在线
./shared/scripts/start-services.sh restart
sleep 15
./shared/scripts/start-services.sh status   # 4 服务全 UP

# 2. admin JWT（浏览器登录后从 LocalStorage 取，或 curl /api/auth/login）
export TEST_ADMIN_JWT='eyJ...'

# 3. 装 jq 如果没有
sudo apt install -y jq

# 4. 清掉旧测试任务（id 120/121/122 是上次 session 的脏数据；本次 commit 后清干净再跑 e2e）
PGPASSWORD=amis_ai_dev psql -h localhost -U amis_ai -d amis_ai -c "
DELETE FROM project_task_message WHERE task_id IN (120, 121, 122);
DELETE FROM project_task_event   WHERE task_id IN (120, 121, 122);
DELETE FROM project_task_page    WHERE task_id IN (120, 121, 122);
DELETE FROM project_generation_task WHERE id IN (120, 121, 122);
"
```

## 项目惯例（每次 session 开始 AI 都要遵守）

- **所有沟通 / 思考 / 注释用中文**，亲切活泼风格
- **未经用户允许不主动 git push / PR / tag**（本地 commit 在 dev 分支 OK）
- **commit message 中文 conventional 风格**（`feat(1.2):` / `fix(1.2):` / `docs(1.2):`）
- **每条 Bash 用绝对路径** `cd /home/karl/Working/TianXing/amis-ai && ...`（避免 cwd 漂移；`run_in_background` 后 cwd 容易回根目录）
- **subagent 不要自己 commit**（触发 harness SECURITY），主 agent 复审通过后 commit
- **subagent 改测试不要破坏 1.1 已落地的 import 风格**（agent/pyproject.toml `pythonpath=[".", "src"]` 已配，测试用 `from src.routers.xxx`）

## 关键代码路径速查

| 关心点 | 文件 |
|---|---|
| 多页调度器 + 5 策略实现 + epilog 闭环 | [`backend/src/services/multipage_scheduler.rs`](../../backend/src/services/multipage_scheduler.rs) |
| 多页 session 真完成等待器（W6.5 缺陷 1）| [`backend/src/services/multipage_session_watcher.rs`](../../backend/src/services/multipage_session_watcher.rs) |
| backend 内联复用率（W6.5 缺陷 4）| [`backend/src/services/multipage_reuse.rs`](../../backend/src/services/multipage_reuse.rs) |
| 多页 prompt 模板（3 个）| [`claw-code/rust/crates/claw-agent-server/prompts/`](../../claw-code/rust/crates/claw-agent-server/prompts/) |
| sandbox 复用率（保留供后续可能的容器内调用）| [`sandbox/src/reuse_metrics.rs`](../../sandbox/src/reuse_metrics.rs) |
| agent 多页 RAG 写入 | [`agent/src/knowledge/multipage_recorder.py`](../../agent/src/knowledge/multipage_recorder.py) |
| RAG strategy filter | [`agent/src/services/rag.py`](../../agent/src/services/rag.py) `search_code_samples` 函数 |
| backend 多页 endpoints | [`backend/src/handlers/project_pages.rs`](../../backend/src/handlers/project_pages.rs) |
| 前端 PagesPanel | [`frontend/src/views/Projects/detail/panels/PagesPanel.tsx`](../../frontend/src/views/Projects/detail/panels/PagesPanel.tsx) |
| 前端多页输入页 | [`frontend/src/views/Projects/CreateMultipageTask.tsx`](../../frontend/src/views/Projects/CreateMultipageTask.tsx) |
| 5 个 e2e 脚本 | [`eval/multipage-1.2/`](../../eval/multipage-1.2/) |

---

## next session 起步 prompt（用户复制粘贴）

> 看 `docs/plans/2026-05-08-1.2-handoff.md` 接着做 1.2.0 收尾。4 项缺陷已修，先跑 5 个 e2e 验证闭环（关键看每页 started_at→finished_at 是真耗时不再 64ms + events 表里有 reuse_metric/rag_recorded），再做 W7.1 评测 runner、W7.2 5 策略对比、W7.3 合 main + tag v1.2.0。
