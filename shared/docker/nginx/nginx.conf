server {
    listen 80;
    server_name localhost;

    # 前端静态资源
    location / {
        proxy_pass http://host.docker.internal:5173;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        # WebSocket 支持（Vite HMR）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Rust 后台 API
    location /api/ {
        proxy_pass http://host.docker.internal:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Python 智能体 API
    location /agent/ {
        proxy_pass http://host.docker.internal:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        # SSE 支持
        proxy_set_header Connection '';
        proxy_http_version 1.1;
        chunked_transfer_encoding off;
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }

    # 禁止外部访问内部 API
    location /api/internal/ {
        return 403;
    }
    location /internal/ {
        return 403;
    }

    # ────────────────────── /preview/{task_id}/ 反代（D.1，生产部署用） ──────────────────────
    #
    # **当前默认禁用**。开发环境下前端 iframe 直连 sandbox 端口（http://localhost:${previewPort}），
    # 不需要 Nginx 反代——简单且 HMR 顺畅。
    #
    # 生产部署（前端跑在另一台机 / HTTPS / 不想暴露 20000+ 端口段）时启用：
    # 1. 装 OpenResty（含 lua-nginx-module）替换原版 nginx
    # 2. 取消下面注释，并把 backend 改造成提供 `GET /api/internal/task-port/:id`
    #    返回 `{port: 20001}`，让 Lua 缓存 task_id → port 映射
    # 3. PreviewPanel 把 iframe src 从 `http://localhost:20001` 改成 `/preview/123/`
    #
    # 样板（OpenResty + Lua）：
    # location ~ ^/preview/(\d+)/(.*)$ {
    #     set $task_id $1;
    #     set $rest $2;
    #     access_by_lua_block {
    #         local r = ngx.location.capture("/__internal_task_port?id=" .. ngx.var.task_id)
    #         if r.status ~= 200 then ngx.exit(404) end
    #         ngx.var.target_port = r.body
    #     }
    #     proxy_pass http://host.docker.internal:$target_port/$rest$is_args$args;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";  # Vite HMR
    # }
}
